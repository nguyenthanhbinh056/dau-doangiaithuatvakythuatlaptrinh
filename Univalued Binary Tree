class Solution:
    def isUnivalTree(self, root):
        if not root:
            return True

        target = root.val

        def dfs(node):
            if not node:
                return True

            if node.val != target:
                return False

            return dfs(node.left) and dfs(node.right)

        return dfs(root)
