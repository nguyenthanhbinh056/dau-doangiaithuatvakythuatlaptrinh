class Solution:
    def removeNthFromEnd(self, head):
        if not head:
            return None

        if not head.next:
            return None

        curr = head

        # đi tới node trước tail
        while curr.next and curr.next.next:
            curr = curr.next

        # xóa node cuối
        curr.next = None

        return head
